# Cloudflare Pages 설정
name = "pi-management"
compatibility_date = "2024-01-01"

[site]
bucket = "./dist"

# 환경별 설정
[env.production]
vars = { ENVIRONMENT = "production" }

[env.staging]
vars = { ENVIRONMENT = "staging" }
